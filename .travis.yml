language: node_js
node_js:
  - "8"
cache:
  yarn: true
  directories:
    - "node_modules"
before_install:
    - yarn install
cache:
  directories:
    - "node_modules"
script:
    - yarn run lint
    - yarn run build